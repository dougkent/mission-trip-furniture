export enum SearchablePlanSortableFieldsEnum {
    id = 'id',
    name = 'name',
    description = 'description',
    pdfS3Key = 'pdfS3Key',
    created = 'created',
    favoritedCount = 'favoritedCount',
    downloadedCount = 'downloadedCount',
    requiredMaterialIds = 'requiredMaterialIds',
    requiredToolIds = 'requiredToolIds',
    planCreatedById = 'planCreatedById',
}
